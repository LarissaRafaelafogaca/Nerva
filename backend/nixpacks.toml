[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm rebuild argon2 @prisma/client @prisma/engines prisma", "npx prisma generate", "npm run build"]

[start]
cmd = "npx prisma migrate deploy && node dist/server.js"
